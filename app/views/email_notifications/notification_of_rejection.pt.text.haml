Caro#{@session.authors.size > 1 ? "s":""} #{@session.authors.map(&:full_name).join(" & ")},


Infelizmente, sua proposta '#{h(@session.title)}' não foi selecionada para integrar o programa da Conferência #{@conference_name}.


Recebemos em torno de 100 propostas este ano. Infelizmente, isso significa que muitas boas propostas não puderam ser aceitas devido à falta de espaço. Você#{@session.authors.size > 1 ? "s":""} pode#{@session.authors.size > 1 ? "m":""} ver o feedback sobre sua proposta fornecido pelos avaliadores e pelos organizadores de trilha acessando o endereço:

= session_reviews_url(@session, :locale => 'pt')


Se tiver#{@session.authors.size > 1 ? "em":""} qualquer dúvida, comentário ou sugestão, não hesite#{@session.authors.size > 1 ? "m":""} em entrar em contato com a Comissão de Programa através do email #{link_to 'agiles2012-program@agiles.org', 'mailto:agiles2012-program@agiles.org'}.


Muito obrigado pela sua proposta. Esperamos que você#{@session.authors.size > 1 ? "s":""} participe#{@session.authors.size > 1 ? "m":""} da conferência este ano e traga#{@session.authors.size > 1 ? "m":""} novas propostas para o próximo ano.


Atenciosamente,

#{"--"}

Comissão de Programa da #{@conference_name}


[Por favor não responda este e-mail. Ele foi enviado através de um endereço não monitorado]
