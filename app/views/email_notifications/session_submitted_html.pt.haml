%p Olá #{h(@session.authors.map(&:full_name).join(" & "))},

%p Obrigado por submeter a proposta de sessão <strong>'#{h(@session.title)}'</strong> para a #{AppConfig[:conference_name]}. Você pode acessar e alterar os detalhes de sua proposta até o fim do prazo de submissão no dia #{l(ImportantDates::SUBMISSION_END_DATETIME)}, acessando o link:

%p= link_to session_url(@session, :locale => 'pt'), session_url(@session, :locale => 'pt')

%p Gratos,

%p Equipe de programa da #{AppConfig[:conference_name]}

%small [Por favor não responda este e-mail. Ele foi enviado através de um endereço não monitorado]
